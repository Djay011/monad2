VITE_API_BASE=http://localhost:4000

# Address of the deployed MonadInscriptionMarket contract.
# Leave empty to fall back to off-chain (DB-only) listings without 5% fee enforcement.
VITE_MARKET_CONTRACT=
